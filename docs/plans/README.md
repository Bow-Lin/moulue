# Plans

This directory is for active or upcoming plans only.

Use `docs/plans/` for:

- current design docs
- current implementation plans
- work that is still being executed or reviewed

Completed plans are moved to:

- `docs/archive/plans/`

If a plan describes work that has already landed, archive it so old plans do not look like current direction.
